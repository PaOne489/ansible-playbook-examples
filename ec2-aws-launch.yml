--- 
- hosts: localhost
  vars:
          key_pair: aws
          sg: web-sg
          region: us-west-2
          image_id: ami-a9d276c9
          instance_type: t2.micro
  tasks:

  - name: launching EC2 Instance in "{{ region }}" with "{{ image_id }}" 
    ec2:
            key_name: "{{ key_pair }}"
            group: "{{ sg }}"
            image: "{{ image-id }}"
            instance_type: "{{ instnace_type }}"
            region: "{{ region }}"
            instance_tags:
                    Name: web-test


